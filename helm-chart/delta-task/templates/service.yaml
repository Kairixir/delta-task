apiVersion: v1
kind: Service
metadata:
  name: {{ include "delta-task.fullname" . }}
  labels:
    {{- include "delta-task.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "delta-task.selectorLabels" . | nindent 4 }}
