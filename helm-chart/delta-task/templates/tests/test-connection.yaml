apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "delta-task.fullname" . }}-test-connection"
  labels:
    {{- include "delta-task.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "delta-task.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
